{
  "name": "@discere-os/ffmpeg.wasm",
  "version": "0.12.15",
  "description": "High-performance FFmpeg video/audio processing with WebAssembly, multi-threading, and TypeScript-first API",
  "exports": {
    ".": "./src/lib/index.ts",
    "./types": "./src/lib/types.ts",
    "./worker": "./src/lib/worker.ts",
    "./core": "./packages/core/dist/esm/ffmpeg-core.js",
    "./core-mt": "./packages/core-mt/dist/esm/ffmpeg-core.js"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.14",
    "@std/testing": "jsr:@std/testing@^1.0.0",
    "@std/async": "jsr:@std/async@^1.0.5",
    "@std/streams": "jsr:@std/streams@^1.0.6",
    "module": "data:application/javascript,export const createRequire = () => (id) => { if (id === 'path') return { dirname: () => '', join: (...args) => args.join('/') }; if (id === 'url') return { fileURLToPath: (url) => url.replace('file://', '') }; if (id === 'fs') return { readFileSync: () => { throw new Error('fs not supported'); } }; throw new Error(`Module ${id} not found`); }; export default { createRequire };",
    "path": "data:application/javascript,export const dirname = () => ''; export const join = (...args) => args.join('/'); export default { dirname, join };",
    "url": "data:application/javascript,export const fileURLToPath = (url) => url.replace('file://', ''); export default { fileURLToPath };",
    "fs": "data:application/javascript,export const readFileSync = () => { throw new Error('fs not supported'); }; export default { readFileSync };",
    "util": "data:application/javascript,export const promisify = fn => (...args) => new Promise((resolve, reject) => fn(...args, (err, result) => err ? reject(err) : resolve(result))); export default { promisify };"
  },
  "tasks": {
    "demo": "deno run --allow-read --allow-write --allow-net demo-deno.ts",
    "demo:video": "deno run --allow-read --allow-write --allow-net demo-video.ts",
    "demo:audio": "deno run --allow-read --allow-write --allow-net demo-audio.ts",
    "test": "deno test --allow-read --allow-write --allow-net --no-check tests/deno/",
    "test:basic": "deno test --allow-read --allow-write --allow-net --no-check tests/deno/basic.test.ts",
    "test:transcoding": "deno test --allow-read --allow-write --allow-net --no-check tests/deno/transcoding.test.ts",
    "test:threading": "deno test --allow-read --allow-write --allow-net --no-check tests/deno/threading.test.ts",
    "benchmark": "deno run --allow-read --allow-write --allow-net bench/ffmpeg.bench.ts",
    "benchmark:video": "deno run --allow-read --allow-write --allow-net bench/video.bench.ts",
    "benchmark:audio": "deno run --allow-read --allow-write --allow-net bench/audio.bench.ts",
    "build": "deno task build:consolidated",
    "build:consolidated": "deno run --allow-all scripts/build-consolidated.ts",
    "build:packages": "npm run build --workspace=packages --if-present",
    "build:npm": "deno run --allow-all _build_npm.ts",
    "build:all": "deno task build:packages && deno task build:consolidated && deno task build:npm",
    "publish:npm": "deno task build:all && cd npm && npm publish",
    "publish:dry": "deno task build:all && cd npm && npm publish --dry-run",
    "serve": "deno run --allow-all --allow-net=:3000 scripts/serve.ts",
    "clean": "rm -rf dist/ build/ packages/*/dist/ npm/ && echo '✅ Cleaned all build artifacts'",
    "check": "deno check src/lib/index.ts",
    "check:all": "deno check src/lib/index.ts && deno check demo-deno.ts && deno check bench/ffmpeg.bench.ts && deno check scripts/*.ts"
  },
  "compilerOptions": {
    "lib": ["deno.ns", "dom", "es2022", "deno.unstable", "webworker"],
    "strict": true,
    "allowUnreachableCode": false,
    "allowUnusedLabels": false,
    "exactOptionalPropertyTypes": true,
    "noFallthroughCasesInSwitch": true,
    "noImplicitOverride": true,
    "noImplicitReturns": true,
    "noPropertyAccessFromIndexSignature": true,
    "noUncheckedIndexedAccess": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true
  },
  "fmt": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["dist/", "build/", "packages/*/dist/", "npm/", "node_modules/"]
  },
  "lint": {
    "include": ["src/", "tests/", "demo*.ts", "bench/", "scripts/"],
    "exclude": ["dist/", "build/", "packages/*/dist/", "npm/", "node_modules/"]
  },
  "test": {
    "include": ["tests/deno/"],
    "exclude": ["tests/unit/", "node_modules/"]
  }
}